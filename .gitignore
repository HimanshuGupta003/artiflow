# Global ignores for node projects
node_modules/
.env # Crucial: do NOT commit your .env files
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.DS_Store
.vscode/ # Optional: if you don't want to commit VS Code settings

# Frontend (client) specific ignores
client/node_modules/
client/build/
client/dist/ # Adjust if your frontend build output folder is 'dist' or 'out'

# Backend (server) specific ignores
server/node_modules/-